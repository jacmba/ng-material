import { DemoRouting.Module } from './demo-routing.module';

describe('DemoRouting.Module', () => {
  it('should create an instance', () => {
    expect(new DemoRouting.Module()).toBeTruthy();
  });
});
